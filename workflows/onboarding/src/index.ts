import { APP_NAME } from "@tminus/shared";

/**
 * OnboardingWorkflow -- orchestrates the initial account setup flow.
 * Placeholder skeleton for Phase 1 implementation.
 */
export class OnboardingWorkflow {
  readonly name = `${APP_NAME}-onboarding`;
}
