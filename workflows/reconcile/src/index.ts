import { APP_NAME } from "@tminus/shared";

/**
 * ReconcileWorkflow -- daily drift reconciliation per AD-6.
 * Placeholder skeleton for Phase 1 implementation.
 */
export class ReconcileWorkflow {
  readonly name = `${APP_NAME}-reconcile`;
}
